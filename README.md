This repository contains the source files for my Hugo-powered blog.

It is deployed via a GitHub Actions workflow that builds the site with Hugo and copies the resulting static files into a webserver container image.
